[pytest]
testpaths = app/src/tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -m "not mt5"
markers =
    mt5: requires a live MetaTrader5 terminal (skipped by default)
