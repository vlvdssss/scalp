# Telegram service dependencies
# Install with: pip install -r requirements_tg.txt

fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-telegram-bot[http2]>=21.0
python-dotenv>=1.0.0

# Already in main requirements.txt — listed here for completeness:
# pyyaml
# requests
